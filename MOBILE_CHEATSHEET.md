# 📱 MOBILE CHEATSHEET - Onsdag 18 Dec

**SKRIV UT ELLER HA PÅ TELEFON!**

---

## ⏰ TIDSLINJE

```
0-5:    Pitcha idé + fördela roller
5-15:   Setup Expo (npx create-expo-app)
15-30:  Börja koda AI
30-60:  Fortsätt AI + förbättra
60:     🚨 MERGE TILL MAIN (deadline!)
60-105: Character customization
105-135: Polish / Bug fixes
135-150: Testing
150-165: Deploy + demo prep
165:    🎤 DEMO!
```

---

## 🚀 SETUP (Min 5-15)

```bash
# Skapa projekt
npx create-expo-app grinch-game --template blank
cd grinch-game

# Install
npm install react-native-game-engine matter-js expo-av @expo/vector-icons @anthropic-ai/sdk

# Start
npx expo start
```

---

## 💻 DIN KOD (Min 15-30)

```typescript
// src/api/claude.ts
import Anthropic from '@anthropic-ai/sdk';

const client = new Anthropic({
  apiKey: process.env.EXPO_PUBLIC_ANTHROPIC_API_KEY,
});

export async function generateLevel(difficulty: string) {
  const message = await client.messages.create({
    model: 'claude-opus-4-5-20251101',
    max_tokens: 2048,
    messages: [{
      role: 'user',
      content: `Generate Christmas platformer level.
      Screen: 375×667. Difficulty: ${difficulty}.
      Return JSON with platforms, gifts, enemies.`
    }],
  });

  return JSON.parse(message.content[0].text);
}
```

---

## 🔑 API KEY

```
YOUR_ANTHROPIC_API_KEY_HERE
```

**Lägg i .env:**
```
EXPO_PUBLIC_ANTHROPIC_API_KEY=sk-ant-api03-...
```

---

## 📋 GIT COMMANDS

```bash
# Din branch
git checkout -b rickard/ai-integration

# Commit (var 20 min)
git add .
git commit -m "ai: beskrivning"
git push origin rickard/ai-integration

# Merge vid minut 60
git checkout main
git pull origin main
git merge rickard/ai-integration
git push origin main
```

---

## 🎤 DEMO SCRIPT

```
"Every level is AI-generated by Claude Opus 4.5.
*Tap Generate Level*
*3 seconds*
*New level appears*

We built this on React Native + Expo -
the same stack Natively.dev uses.
Native mobile. AI-driven. 2.5 hours."
```

---

## ⚠️ TROUBLESHOOTING

**API slow?**
→ Use fallback level

**Can't merge?**
→ Push to your branch, Person 5 merges

**Expo crash?**
→ `npx expo start -c`

**Blocked?**
→ Ask Person 5

---

## ✅ SUCCESS CRITERIA

**MUST:**
- AI level gen works @ min 60
- Merged to main
- < 5 sec response
- Fallback exists

**NICE:**
- Character customization
- Cache
- Difficulty adaptation

---

## 🏆 FOCUS

**1st Priority:**
Level generation (CORE)

**2nd Priority:**
Character customization

**3rd Priority:**
Polish / Help others

---

**REDO ATT VINNA! 🚀**
